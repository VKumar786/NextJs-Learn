/*
    TODO: GET API with Static Data in API Routes
    ? API for user details and user list
    ? make a static db file
    ? make user list API
    ? make a route for single-user API.
    ? get request params
    ? filter result and test API
*/