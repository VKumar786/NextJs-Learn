/*
    TODO: Functions | state in next.Js
    ? what is `use client` in next.js?
    ? how to call function in next.js?
    ? make a component inside a components?
    ? call component as a function?
    ? what is a state in React.js? 

    ? diff b/w state and variable is that state will re-render it self when it is changed
    ? inner Component
    ? diff b/w  <Hello /> & {Hello()}, we use component b/c they have life cycle method, where function lags
*/