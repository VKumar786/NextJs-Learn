/*
    TODO: Link and Navigation
    ? two ways of navigation between routes
    ? Difference between link and navigation's

    ? Linking is going from one page to another page using Anchor tag
    ? Navigation is going from one page to another page using Button tag
*/