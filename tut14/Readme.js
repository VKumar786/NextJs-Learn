/*
    TODO: client side vs pre-rendering
    ? how to know rendering type
        * React.js [client side rendering] - it means processing is done at client side, localhost + js
        * Next.js [server side rendering] - it means processing is done at server side, localhost
    ? check result of rendering
        * open Inspect -> network tab -> select type doc -> go to preview
    ? why this is important
    ? SEO with rendering
    ? How is SEO works
*/  