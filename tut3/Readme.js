/*
    TODO: File and folder | Project structure | server component | Client component
    ? package.json - project command, all details of dependencies

    ? component type based on rendering
        * 1. Server Components
        * 2. Client Components

    ? Server Components
        * Render on Server Side
        * By default, All Components are server components.
        * They are closer to backend
        * so Be-related logic and code should write in server components
    ? Client Components
        * Render on Client Side
        * Can make client components with
        * `use client` directive.
        * all UI-related code and events should be written here.
*/